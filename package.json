{
  "name": "quick-vint",
  "version": "1.0.0",
  "description": "",
  "main": "content.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "API_BASE_URL=https://quick-vint.vercel.app node scripts/set-env.js && terser content.js -o content.min.js --compress --mangle",
    "build:prod": "API_BASE_URL=https://quick-vint.vercel.app node scripts/set-env.js && terser content.js -o content.min.js --compress --mangle",
    "build:staging": "API_BASE_URL=https://quick-vint-git-new-pricing-ahmed-samis-projects-e6ef0336.vercel.app node scripts/set-env.js && terser content.js -o content.min.js --compress --mangle",
    "build:local": "API_BASE_URL=http://localhost:5000 node scripts/set-env.js && terser content.js -o content.min.js --compress --mangle",
    "prepare": "npm run build",
    "package": "node build.js",
    "package:bash": "./build.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/samicodesit/quick-vint-frontend.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/samicodesit/quick-vint-frontend/issues"
  },
  "homepage": "https://github.com/samicodesit/quick-vint-frontend#readme",
  "devDependencies": {
    "terser": "^5.39.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.8"
  }
}
